<a class="user-component" href="{{$song->url}}">
    <div class="user-component-avatar">
        <img src="{{$song->img}}" alt="{{$song->title}}">
    </div>
    <div class="user-component-info">
        <p class='name'>{{$song->title}}</p>
    </div>
</a>